diff --git a/.goreleaser.yaml b/.goreleaser.yaml
index 2b603a8..e226df6 100644
--- a/.goreleaser.yaml
+++ b/.goreleaser.yaml
@@ -1,5 +1,6 @@
 # yaml-language-server: $schema=https://goreleaser.com/static/schema.json
 version: 2
+project_name: continuous
 
 before:
   hooks:
@@ -42,11 +43,12 @@ archives:
 
 brews:
   - name: continuous
+    directory: Formula
+    skip_upload: true
     repository:
       owner: continuous-labs-ai
       name: homebrew-tap
       branch: main
-    token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
     homepage: "https://github.com/continuous-labs-ai/cli"
     description: "Continuous Simulation API: Build Simulators from OpenAPI or WSDL documents, create Simulations from them, and build Worlds that run Simulations together. Authenticate every request with an API key sent as a Bearer token."
     install: |
@@ -59,6 +61,8 @@ release:
   draft: false
   prerelease: auto
   mode: append
+  extra_files:
+    - glob: dist/homebrew/Formula/continuous.rb
 
 checksum:
   name_template: "checksums.txt"
